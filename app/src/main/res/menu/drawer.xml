<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group
        android:checkableBehavior="single">

        <item
            android:id="@+id/drawer_home"
            android:checked="true"
            android:icon="@drawable/options"
            android:title="Home"/>

        <item
            android:id="@+id/drawer_test"
            android:icon="@drawable/options"
            android:title="Test Online"/>

        <item
            android:id="@+id/drawer_about"
            android:icon="@drawable/options"
            android:title="About"/>

        <item
            android:id="@+id/drawer_history"
            android:icon="@drawable/options"
            android:visible="false"
            android:title="History"/>

        <item android:id="@+id/drawer_logout"
            android:visible="false"
            android:icon="@drawable/options"
            android:title="Logout"/>

        <item android:id="@+id/drawer_saved_questions"
            android:visible="false"
            android:icon="@drawable/options"
            android:title="Saved questions"/>

        <item android:id="@+id/drawer_change_password"
            android:visible="false"
            android:icon="@drawable/options"
            android:title="Change Password"/>

    </group>
</menu>